# Normalize line endings: text files as LF in the repo (EN/ES page pairs had
# drifted to mixed CRLF/LF). Binary assets are left untouched.
* text=auto
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.mp4 binary
